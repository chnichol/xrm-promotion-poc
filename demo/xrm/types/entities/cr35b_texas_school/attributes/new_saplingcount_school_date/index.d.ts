import { Attribute } from "xrm-types";
import Entity from "../../index";

/** Last Updated time of rollup field Sapling Count. */
export default interface new_saplingcount_school_date extends Attribute<Entity["new_saplingcount_school_date"]> {
}
