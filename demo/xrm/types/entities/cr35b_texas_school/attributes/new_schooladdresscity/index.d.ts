import { Attribute } from "xrm-types";
import Entity from "../../index";

/** The city the school is in. */
export default interface new_schooladdresscity extends Attribute<Entity["new_schooladdresscity"]> {
}
