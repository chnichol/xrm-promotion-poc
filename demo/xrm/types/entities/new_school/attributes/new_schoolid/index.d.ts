import { Attribute } from "xrm-types";
import Entity from "../../index";

/** Unique identifier for entity instances */
export default interface new_schoolid extends Attribute<Entity["new_schoolid"]> {
}
