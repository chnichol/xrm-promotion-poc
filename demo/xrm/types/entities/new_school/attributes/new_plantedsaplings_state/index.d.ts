import { Attribute } from "xrm-types";
import Entity from "../../index";

/** State of rollup field Planted Saplings. */
export default interface new_plantedsaplings_state extends Attribute<Entity["new_plantedsaplings_state"]> {
}
