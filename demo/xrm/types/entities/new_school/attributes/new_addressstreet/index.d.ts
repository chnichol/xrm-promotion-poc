import { Attribute } from "xrm-types";
import Entity from "../../index";

/** Street number and name for the school address. */
export default interface new_addressstreet extends Attribute<Entity["new_addressstreet"]> {
}
