import { Attribute } from "xrm-types";
import Entity from "../../index";

/** For internal use only. */
export default interface timezoneruleversionnumber extends Attribute<Entity["timezoneruleversionnumber"]> {
}
