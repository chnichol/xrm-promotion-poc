import { Attribute } from "xrm-types";
import Entity from "../../index";

/** State of rollup field Sapling Count. */
export default interface new_saplingcount_state extends Attribute<Entity["new_saplingcount_state"]> {
}
