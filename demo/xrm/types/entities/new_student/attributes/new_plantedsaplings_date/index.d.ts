import { Attribute } from "xrm-types";
import Entity from "../../index";

/** Last Updated time of rollup field PlantedSaplings. */
export default interface new_plantedsaplings_date extends Attribute<Entity["new_plantedsaplings_date"]> {
}
