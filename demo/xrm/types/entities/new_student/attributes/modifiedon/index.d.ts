import { Attribute } from "xrm-types";
import Entity from "../../index";

/** Date and time when the record was modified. */
export default interface modifiedon extends Attribute<Entity["modifiedon"]> {
}
